#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: a param;
 * return: returns a value
 */

int _strlen(char *s)
{
	int len;
	len = strlen(s);
	return (len);
}
